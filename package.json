{
  "name": "parcel-bug",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "watch": "parcel watch --target app",
    "build": "parcel build --target app --no-optimize",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.4.1"
  },
  "browserslist": [
    "> 0.5%",
    "not dead",
    "last 2 versions"
  ],
  "targets": {
    "app": {
      "context": "browser",
      "source": "src/app/index.html",
      "distDir": "dist/app"
    }
  }
}
